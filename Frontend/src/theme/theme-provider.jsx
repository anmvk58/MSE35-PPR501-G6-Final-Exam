import React from 'react';

export function ThemeProvider({ children }) {
  // Simple theme provider placeholder
  // You can extend this later with actual theme functionality
  return <>{children}</>;
}

export default ThemeProvider;